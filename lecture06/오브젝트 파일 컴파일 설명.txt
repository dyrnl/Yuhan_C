// 오브젝트 파일로 만들기
// 설명:  -c 는 링크를 안한다는 뜻 / -o 는 바로 뒤에 있는 파일로 출력한다는 뜻 / 만들파일.o 뒤에 원본파일
// 이걸 모든 c파일에 똑같이 해서 전부 o파일로 만들어주기
gcc -c -o 만들파일.o 대상파일.c 

// 만들어진 o파일을 모두 .exe 파일으로 링크해서 만들기
// 근데 .o 파일은 무조건 main함수가 있는 .o파일이 가장 앞으로 오게
gcc -o 실행파일.exe 만들어진파일.o 만들어진파일.o 만들어진파일.o ... (전부)

